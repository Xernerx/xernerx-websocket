<!-- @format -->

# Changesets

This directory contains changeset files. When you want to release a new version of the package, run:

```
npx changeset
```

and follow the prompts. The changeset files will be committed and used to bump versions later using:

```
npx changeset version
```
